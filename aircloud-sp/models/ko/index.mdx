---
title: "사용 가능한 모델"
sidebarTitle: "모델"
description: "AirCloud에서 사용 가능한 AI 모델을 살펴보세요."
mode: "wide"
---

AirCloud는 다양한 AI 모델을 제공합니다. 모든 모델은 OpenAI 호환 인터페이스를 통해 Air API로 접근할 수 있습니다.

## 텍스트 모델

대화형 AI, 추론, 코드 생성.

| 모델 | 설명 | 입력 | 출력 |
|------|------|------|------|
| [Qwen3.8-27B](/models/ko/qwen3-8-27b) | 에이전트 작업에 강한 소형 dense 비전-언어 모델. | \$0.400/1M 토큰 | \$3.000/1M 토큰 |
| [Qwen3.6-35B-A3B](/models/ko/qwen3-6-35b-a3b) | 멀티모달을 지원하는 고성능 MoE 모델. | \$0.140/1M 토큰 | \$1.000/1M 토큰 |
| [Qwen3.5-9B](/models/ko/qwen3-5-9b) | 멀티모달을 지원하는 컴팩트하고 효율적인 모델. | \$0.100/1M 토큰 | \$0.150/1M 토큰 |
| [Qwen3-32B](/models/ko/qwen3-32b) | 하이브리드 추론을 지원하는 32B dense 모델. | \$0.080/1M 토큰 | \$0.280/1M 토큰 |
| [Llama3.3-70B](/models/ko/llama3-3-70b) | 범용 작업을 위한 Meta의 다국어 70B instruct 모델. | \$0.100/1M 토큰 | \$0.320/1M 토큰 |
| [DeepSeek V4 Flash](/models/ko/deepseek-v4-flash) | 빠르고 저렴한 추론에 최적화된 MoE 모델. | \$0.100/1M 토큰 | \$0.200/1M 토큰 |

## 임베딩 모델

시맨틱 검색과 RAG를 위한 벡터 임베딩.

| 모델 | 설명 | 입력 | 출력 |
|------|------|------|------|
| [Qwen3-Embedding-8B](/models/ko/qwen3-embedding-8b) | 4,096차원 벡터를 지원하는 다국어 임베딩 모델. | \$0.010/1M 토큰 | \$0.000/1M 토큰 |

## 오디오 모델

텍스트 음성 변환 및 음성 합성.

| 모델 | 설명 | 입력 | 출력 |
|------|------|------|------|
| [Qwen3-TTS](/models/ko/qwen3-tts) | 커스텀 보이스 클로닝을 지원하는 다국어 TTS 모델. | \$0.080/1,000자 | \$0.000 |

## 시작하기

<Steps>
  <Step title="API 키 발급하기">
    [AirCloud 계정](https://aieev.com)에서 API 키를 발급받습니다.
  </Step>
  <Step title="첫 번째 요청 보내기">
    아래 코드에서 `YOUR_API_KEY` 자리에 실제 발급받은 키를 넣어 실행하세요. 언어별(Python, cURL, Node.js) 코드 예시를 선택할 수 있습니다.
  </Step>
</Steps>

<CodeGroup>
```python Python
import requests

response = requests.post(
    "https://ap-1.aieev.cloud/endpoints/qwen3-5-9b/v1/chat/completions",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "model": "qwen/qwen3.5-9b",
        "messages": [{"role": "user", "content": "Hello!"}]
    }
)

result = response.json()
print(result["choices"][0]["message"]["content"])
```

```bash cURL
curl --request POST \
  --url https://ap-1.aieev.cloud/endpoints/qwen3-5-9b/v1/chat/completions \
  --header 'Authorization: Bearer YOUR_API_KEY' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "qwen/qwen3.5-9b",
    "messages": [{"role": "user", "content": "Hello!"}]
  }'
```

```javascript Node.js
const response = await fetch(
  "https://ap-1.aieev.cloud/endpoints/qwen3-5-9b/v1/chat/completions",
  {
    method: "POST",
    headers: {
      "Authorization": "Bearer YOUR_API_KEY",
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      model: "qwen/qwen3.5-9b",
      messages: [{ role: "user", content: "Hello!" }]
    })
  }
);

const result = await response.json();
console.log(result.choices[0].message.content);
```
</CodeGroup>
